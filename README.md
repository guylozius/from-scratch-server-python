# From Scratch Server (Python)

A raw-socket server built from scratch in Python (no frameworks). Learning project focused on networking fundamentals.

## Goals
- Accept TCP connections
- Parse basic requests (start with HTTP)
- Send responses
- Add concurrency (threads/selectors)
